---
compass_area: "{{AREA_NAME}}"
area_path: "{{AREA_PATH}}"
generated: "{{GENERATED_AT}}"
# Staleness stamp — machine-readable so a refresh can test drift without a model.
# `sources` are area-relative paths (relative to THIS file's directory): the exact
# files the map below was written from. `sources_hash` is the canonical hash over
# their contents (see compass-hash.mjs). Recompute with:
#   node <skill>/compass-hash.mjs path/to/COMPASS.md
# Fill {{SOURCES_LIST}} as a YAML list, one `  - relpath` per line.
sources_hash: "{{SOURCES_HASH}}"
sources:
{{SOURCES_LIST}}
---

# Compass: {{AREA_NAME}}

> Tribal-knowledge map for `{{AREA_PATH}}`. The *why* and the *gotchas* — not the *what*;
> read the code for *what*. Generated by `project-compass`; refresh when this area
> changes materially (the frontmatter stamp above makes drift testable). Pair it with
> the code, not with AGENTS.md, which only indexes it.

## Purpose

<!-- What this area is responsible for, in 2-3 sentences. The role it plays in the
     whole system, not a file listing. -->

{{PURPOSE}}

## Key files & entry points

<!-- The handful of files that matter for understanding this area, and why each matters.
     Not every file — the ones a newcomer should open first. -->

{{ENTRY_POINTS}}

## How it connects

<!-- Upstream callers and downstream dependencies. What flows in, what flows out, and
     across which boundaries (process, network, async). -->

{{CONNECTIONS}}

## Gotchas & non-obvious constraints

<!-- The tribal knowledge: ordering requirements, shared mutable state, implicit
     contracts, "looks wrong but is load-bearing" code. The things that aren't in the
     code's surface and that cost someone a debugging session to learn. -->

{{GOTCHAS}}

## Failure modes seen here

<!-- Concrete incidents and the conditions that triggered them. When one of these
     generalizes beyond this area, promote it to AGENTS.md and link back here. -->

{{FAILURE_MODES}}
