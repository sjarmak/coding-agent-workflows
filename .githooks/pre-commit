#!/usr/bin/env bash
# Block commits when rendered targets drift from source/ (the repo's one
# mechanical invariant). Regenerate with: npm run build
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
npm run check
